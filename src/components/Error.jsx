

const Error = ({mensaje}) => {
    return (
        <div>
                 <p className="bg-violet-100 text-red-600 text-center uppercase font-bold rounded-xl">{mensaje}</p>
            </div>
    )
}

export default Error
